#include <stdio.h>

int main()
{
	char* article[] = {"den", "en", "nogle", "enhver, ”det", "flere"};

	char* noun[] = {"personer", "studerende", "arbejdsløse", "by",  "og", "cykel", "s-tog"}; 

	char* verb[]={ "kørte", "hoppede", "løb", "gik", "sad", "bevaegede"};

	char* preposition[]={"til", "fra", "over", "under", "på", "ved siden af"};

	printf("Hello World!\n");

	return (0);
}
